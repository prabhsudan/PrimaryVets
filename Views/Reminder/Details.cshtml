<style>
    .detail-header {
        background: linear-gradient(135deg, #0d6efd, #6ea8fe);
        border-radius: 16px;
        padding: 20px;
        color: white;
        display: flex;
        align-items: center;
        gap: 15px;
    }

    .icon-box {
        width: 55px;
        height: 55px;
        background: rgba(255,255,255,0.2);
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
    }

    .detail-card {
        border-radius: 16px;
        border: none;
        box-shadow: 0 6px 18px rgba(0,0,0,0.08);
    }

    label {
        font-size: 12px;
        color: #888;
        margin-bottom: 2px;
        display: block;
    }

    p {
        font-weight: 500;
        margin-bottom: 0;
    }
</style>
<div class="container mt-4">

    <!-- HEADER -->
    <div class="detail-header shadow-sm">
        <div class="icon-box">
            <i class="bi bi-heart-pulse-fill"></i>
        </div>
        <div>
            <h4 class="mb-1">Final Checkup</h4>
            <small class="text-light opacity-75">Lily – Recovery review</small>
        </div>
    </div>

    <!-- MAIN CARD -->
    <div class="card detail-card mt-4">
        <div class="card-body">

            <!-- INFO GRID -->
            <div class="row g-4">

                <div class="col-md-6">
                    <label>Date</label>
                    <p><i class="bi bi-calendar"></i> 20 May 2026</p>
                </div>

                <div class="col-md-6">
                    <label>Time</label>
                    <p><i class="bi bi-clock"></i> 4:30 PM</p>
                </div>

                <div class="col-md-6">
                    <label>Treatment</label>
                    <p><i class="bi bi-geo-alt"></i> Check Up</p>
                </div>

                <div class="col-md-6">
                    <label>Status</label>
                    <p><span class="badge bg-warning text-dark">Upcoming</span></p>
                </div>

            </div>

            <!-- DESCRIPTION -->
            <hr />
            <div>
                <label>Description</label>
                <p class="text-muted">
                    This is the final recovery checkup after surgery.
                    Ensure proper healing and monitor vitals.
                </p>
            </div>

            <!-- NOTES -->
            <div class="mt-3">
                <label>Notes</label>
                <p class="text-muted">
                    Pet is recovering well. Keep activity limited.
                </p>
            </div>

        </div>
    </div>

    <!-- ACTION BUTTONS -->
    <div class="d-flex gap-3 mt-4">
        <a href="/Reminder/Edit" class="btn btn-outline-primary w-50">
            <i class="bi bi-pencil"></i> Edit
        </a>

        <button class="btn btn-success w-50">
            <i class="bi bi-check-circle"></i> Mark as Done
        </button>
    </div>

</div>