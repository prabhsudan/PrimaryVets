@using System.Text.RegularExpressions

<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        .stat-card-link {
            text-decoration: none !important;
            color: inherit !important;
            display: block;
        }

            .stat-card-link:hover {
                text-decoration: none !important;
                color: inherit !important;
            }

        .stat-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .stat-card-link:hover .stat-card {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2) !important;
        }
    </style>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PrimeVets</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @await RenderSectionAsync("VendorStyles", required: false)
</head>

@{
        var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
        var currentAction = ViewContext.RouteData.Values["action"]?.ToString();

        var role = Context.Session.GetString("UserRole");
        var Name = Context.Session.GetString("Name") ?? "User";
}

<body>
     <div class="topbar-left">
         <button class="menu-btn" id="menuToggle"> ☰ </button> 
     </div>
    <div class="dashboard">
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <a href="/Dashboard/Index" class="stat-card-link">

                    <div class="logo-section">
                        <img src="~/images/Prime_vets_logo.png"
                             alt="Logo" />

                        <h5>PrimeVets</h5>
                    </div>
                </a>
                @* <div class="logo-section">
                    <img src="~/images/Prime_vets_logo.png"
                         alt="Logo" />

                    <h5>PrimeVets</h5>
                </div> *@

            </div>

            @* <ul>

                <li class="@(currentController == "Dashboard" ? "active" : "")">
                    <a asp-controller="Dashboard" asp-action="Index">

                        <i class="bi bi-house-fill"></i>

                        <span>Dashboard</span>
                    </a>
                </li>

            </ul> *@

             <ul>
                <li class="@(currentController == "PetRegistration" ? "active" : "")">
                    <a href="#registrationSubmenu" data-bs-toggle="collapse" class="dropdown-toggle" style="display: flex; justify-content: space-between; align-items: center; width: 100%;">
                        <div>
                            <i class="bi bi-envelope-paper-fill"></i>
                            <span>Pets</span>
                        </div>
                    </a>

                    <ul id="registrationSubmenu" class="collapse @(currentController == "PetRegistration" ? "show" : "")" style="list-style: none; padding-left: 25px; margin-top: 5px;">

                        <li class="@(currentController == "PetRegistration" && currentAction == "Create" ? "active-sub" : "")">
                            <a asp-controller="PetRegistration" asp-action="Create" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Registration</span>
                            </a>
                        </li>

                        <li class="@(currentController == "PetRegistration" && currentAction == "Index" ? "active-sub" : "")">
                            <a asp-controller="PetRegistration" asp-action="Index" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Records</span>
                            </a>
                        </li>

                    </ul>
                </li>
             </ul>
            <ul>
                <li class="@(currentController == "Appointment" ? "active" : "")">
                    <a href="#appointmentSubmenu" data-bs-toggle="collapse" class="dropdown-toggle" style="display: flex; justify-content: space-between; align-items: center; width: 100%;">
                        <div>
                            <i class="bi bi-envelope-paper-fill"></i>
                            <span>Appointments</span>
                        </div>
                    </a>

                    <ul id="appointmentSubmenu" class="collapse @(currentController == "Appointment" ? "show" : "")" style="list-style: none; padding-left: 25px; margin-top: 5px;">
                        <li class="@(currentController == "Appointment" && currentAction == "Create" ? "active-sub" : "")">
                            <a asp-controller="Appointment" asp-action="Create" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Book Appointment</span>
                            </a>
                        </li>

                        <li class="@(currentController == "Appointment" && currentAction == "Index" ? "active-sub" : "")">
                            <a asp-controller="Appointment" asp-action="Index" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Appointments</span>
                            </a>
                        </li>

                    </ul>
                </li>
            </ul>

            <ul>
                <li class="@(currentController == "Reminder" ? "active" : "")">
                    <a asp-controller="Reminder" asp-action="Index">
                        <i class="bi bi-alarm-fill"></i>
                        <span>Reminders</span>
                    </a>
                </li>

            </ul>

            <ul>
                <li class="@(currentController == "Staff" ? "active" : "")">
                    <a asp-controller="Staff" asp-action="Index">
                        <i class="bi bi-person-badge-fill"></i>
                        <span>Staff</span>
                    </a>
                </li>
            </ul>

            <ul>
                <li class="@(currentController == "Vaccination" ? "active" : "")">
                    <a href="#vaccineSubmenu" data-bs-toggle="collapse" class="dropdown-toggle" style="display: flex; justify-content: space-between; align-items: center; width: 100%;">
                        <div>
                            <i class="bi bi-envelope-paper-fill"></i>
                            <span>Vaccination</span>
                        </div>
                    </a>

                    <ul id="vaccineSubmenu" class="collapse @(currentController == "Vaccination" ? "show" : "")" style="list-style: none; padding-left: 25px; margin-top: 5px;">

                        <li class="@(currentController == "Vaccination" && currentAction == "Index" ? "active-sub" : "")">
                            <a asp-controller="Vaccination" asp-action="Index" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Vaccination Record</span>
                            </a>
                        </li>

                        <li class="@(currentController == "Vaccination" && currentAction == "VaccinationList" ? "active-sub" : "")">
                            <a asp-controller="Vaccination" asp-action="VaccinationList" style="font-size: 0.9rem; padding: 5px 0; display: block;">
                                <span>Vaccination List</span>
                            </a>
                        </li>

                    </ul>
                </li>
            </ul>



        </aside>
        <main class="content">
            @RenderBody()
        </main>

    </div>
    <script>
         document.getElementById("menuToggle").addEventListener("click", function () {
            document.getElementById("sidebar").classList.toggle("active");
        });

        // Session Timeout
        var timeoutMinutes = 15;
        var timeoutMilliseconds = timeoutMinutes * 60 * 1000;
        var inactivityTimer;

        function resetTimer() {
            clearTimeout(inactivityTimer);

            inactivityTimer = setTimeout(function () {
                alert("Session expired due to inactivity.");
                window.location.href = "/Auth/Login";
            }, timeoutMilliseconds);
        }

        window.onload = resetTimer;
        document.onmousemove = resetTimer;
        document.onclick = resetTimer;
        document.onscroll = resetTimer;
        document.addEventListener("keydown", resetTimer);
    </script>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>